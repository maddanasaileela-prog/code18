#include <stdio.h>

int main() {
    int a,b,sum=0;
    // Write C code here
    printf("Enter any two integer Numbers");
    scanf("%d,%d",&a,&b);
    sum=a+b;
   printf("The sum of %d and %d is %d",a,b,sum);

    return 0;
}